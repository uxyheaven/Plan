# Plan

这个仓库用于存放各类方案与设计资料。每个独立方案放在一个文件夹中，该文件夹下的 `README.md` 用于描述该方案。

## 目录结构（示例）

- `analysis-system/` - 分析洞察系统。

## 使用建议

1. 每个方案使用独立目录，并在该目录下维护一个 README 说明设计、组件、运行方式。
2. 对于可运行的原型（例如前端页面、微服务），可以在对应目录内提供最小可用的代码与说明。
